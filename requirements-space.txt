# Hugging Face Space runtime pins. The HF Spaces image ships an older
# resolver that mis-resolves transitive deps without these caps.
pydicom>=2.4
huggingface_hub<0.26.0  # 0.26 changed cached_download signature gradio 4.x still calls

